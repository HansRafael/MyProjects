#include <stdio.h>
#include <stdlib.h>
#include "Lista.h"

int main(){
    int cont=0,para=1;
    struct alunx{  
    int matricula;
    char nome[20];
    int presenca;
    };

    Lista* li = make_lista(); //ponteiro p tipo lista
    while (para)
    {
        printf("Di")
        
    }
    

    int x = tam_lista(li);
    
    

    if (list_cheia(li));


    free_lista(li); 
}